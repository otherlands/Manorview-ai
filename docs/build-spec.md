# Build Spec — manorview-ai

- vLLM exposes Prometheus metrics on `/metrics`. 
- Power Automate can receive a webhook using "When an HTTP request is received". 
- Power Automate can post to Teams using "Post a message in a chat or channel" (Flow bot). 
